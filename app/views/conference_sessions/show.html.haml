%h1= @conference_session.summary
.metadata
  .presenters.meta
    = @conference_session.presenters
  .time.meta
    = time_link(@conference_session.start_time, format_time(@conference_session, :include_day => true))
.details
  .description
    = simple_format @conference_session.description, {}, :sanitize => false
    -# %pre= @conference_session.description
  .attendees
    - unless @conference_session.attendees.empty? || (@conference_session.attendees.length == 1 and @conference_session.attendees.include? current_user)
      %h3 Who's Going?
      %ul
        - friend_list( @conference_session ).each do |friend|
          %li= friend


