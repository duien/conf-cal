page << <<-END
  $("#conference_session_#{@conference_session.id} > .description").html('#{escape_javascript(simple_format(@conference_session.description, {}, :sanitize => false))}').slideDown();
END
